# CLAUDE.md

Read `AGENTS.md` first. It is the shared source of project architecture, commands,
and implementation rules for every coding agent.

The highest-risk code is `backend/app/streaks.py`; change it only with matching
edge-case tests. The iOS project is generated by XcodeGen from `ios/project.yml`.

